# API URL for the Vue.js frontend
# In development (npm run dev): defaults to http://localhost:5000
# In production (docker): change to your ngrok URL or deployed backend URL
VITE_API_URL=http://localhost:5000
